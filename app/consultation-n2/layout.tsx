// Layout minimaliste pour la consultation N2 — branche indépendante du sondage principal
export default function ConsultationLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
